---
import { SITE_TITLE } from '../../../constants/config';

import logo from './logo.jpg';

interface Props {
  class?: string;
}

const { class: customClass } = Astro.props;
---

<img class:list={['size-full', customClass]} src={logo.src} alt={SITE_TITLE} />
